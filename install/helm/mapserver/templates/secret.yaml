apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mapserver.fullname" . }}
  labels:
    {{- include "mapserver.labels" . | nindent 4 }}
type: Opaque
stringData:
  OLLEBO_KEY: {{ .Values.ollebo.key | quote }}
